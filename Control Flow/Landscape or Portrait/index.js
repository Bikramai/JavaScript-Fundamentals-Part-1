// write a function of islandscape, it takes two parameters, width, and height of an image, 
// and returns true, if the image is landscape, 
// which means width is greater than heigth, otherwise it returns false.


let isLandscapes = max(600, 500);
console.log(isLandscapes)


function max(width, length) {
    return(width > length);
}
