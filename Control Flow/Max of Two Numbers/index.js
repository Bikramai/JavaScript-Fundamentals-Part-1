// Write a function that takes two numbers and returns the maximun of the two.

let numbers = max(4, 10);
console.log(numbers);


function max(a, b) {
    // if (a > b) return a;
    // //.....
    // return b;

    return (a > b) ? a : b;
}
