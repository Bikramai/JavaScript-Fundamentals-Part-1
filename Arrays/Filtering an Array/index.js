// How to filter the elements of an arrays in javascript?


const numbers = [ 1, -1, 2, 3, 4, 5];

const filtered = numbers.filter(value => value >= 0);  // we can replace value with n coz we are dealing with number.

console.log(filtered);


